# Proxy Inverso

> **Unificado (2026-06-13):** el detalle canónico del proxy (VM, upstreams
> PROD/DEV, dominios, sincronización repo↔VM, reglas de mantención) vive en
> [`arquitectura/PROXY_INVERSO.md`](arquitectura/PROXY_INVERSO.md). Este archivo
> quedó como puntero para no duplicar — la versión anterior tenía el backend
> PROD desactualizado (`:9000`, modelo viejo ya migrado a `9001/9005/9006`).
